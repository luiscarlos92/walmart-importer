# Quick run via CMD

- Install:
  - Double-click `installer.cmd` (or run it from CMD).
- Run main:
  - Double-click `run_main.cmd` and enter `YYYY-MM` when prompted,
    or run from CMD: `run_main.cmd 2025-10`.
